import React from 'react';
import './PrimaryButton.css';


const PrimaryButton = ({buttontext}) => {
  return (
        
        <div className="primaryButton">{buttontext}</div>
    );
};

export default PrimaryButton;
