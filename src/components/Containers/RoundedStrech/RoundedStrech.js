import React from 'react';
import './RoundedStrech.css';

const RoundedStrech = ({content}) => {
  return (
        <div className="roundedWrapper">{content}</div>
    );
};

export default RoundedStrech;
